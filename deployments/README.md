# Deployment records

`base-sepolia.json` is created once by the guarded deployment script and must be
reviewed before commit. Records contain public addresses and transaction data,
never RPC URLs or keys. Mainnet deployment is intentionally unsupported.
