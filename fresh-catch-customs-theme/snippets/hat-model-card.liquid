<article class="hat-card reveal">
  <p class="eyebrow">{{ model }}</p>
  <h3>{{ title }}</h3>
  <p>{{ description }}</p>
  {% if bullets != blank %}<div>{{ bullets }}</div>{% endif %}
  {% render 'button', label: button_label, url: button_url %}
</article>
