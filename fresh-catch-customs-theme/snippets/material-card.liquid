<article class="media-card reveal">
  {% render 'responsive-image', image: image, fallback_asset: fallback_asset, alt: title, width: 1254, height: 1254 %}
  <div class="media-card__content"><p class="eyebrow">{{ guidance }}</p><h3>{{ title }}</h3><p>{{ description }}</p><a href="{{ url }}">Learn more →</a></div>
</article>
